# ─────────────────────────────────────────────────────────────────
# Brain Tumor Classifier — EfficientNet-B4 + Grad-CAM Dashboard
# ─────────────────────────────────────────────────────────────────

# Core ML
torch>=2.1.0
torchvision>=0.16.0
timm>=0.9.12

# Explainability
grad-cam>=1.4.8           # pytorch-grad-cam (install as: pip install grad-cam)

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0

# Numerical
numpy>=1.24.0

# Visualization & evaluation
matplotlib>=3.8.0
seaborn>=0.13.0
scikit-learn>=1.3.0

# Dashboard backend
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9   # required for FastAPI file uploads

# ─────────────────────────────────────────────────────────────────
# Install order (to avoid conflicts):
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install timm grad-cam Pillow opencv-python numpy
#   pip install matplotlib seaborn scikit-learn
#   pip install fastapi uvicorn[standard] python-multipart
# ─────────────────────────────────────────────────────────────────
